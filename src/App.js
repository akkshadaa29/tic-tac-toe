
import './App.css';
import Grid from './Components/Grid/Grid';

function App() {
  return (
    <div>
    <Grid/>  
    </div>
  );
}

export default App;
/*
1.set current=X
if current is x set it to O.
if current is O set it to x.


every grid block is a button
onclick--> print.and change state. take input the grid number and update that also.

*/